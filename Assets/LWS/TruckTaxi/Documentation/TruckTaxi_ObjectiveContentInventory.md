# Authored Objective Content Inventory

Generated from current assets by Truck Taxi / Write Objective Content Inventory. Runtime capabilities still gate these authored references. See TruckTaxi_ObjectiveAudit.md for evaluators, failure conditions, conflicts and validation.

## taxi.objective.FastDelivery
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/FastDelivery.asset`

Authored description: Beat the clock. Runtime text uses authoritative Target.

Enabled: True; deadline: 75s; base target: 1; requirements: Timer; requires/forbids: None / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Max Volume (`passenger-009ae3a402da`), Bill Rush (`passenger-488bd780fe51`), Pat Pending (`passenger-ca28f1b00a01`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.Shortcut
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/Shortcut.asset`

Authored description: Use a shortcut. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: Shortcuts; requires/forbids: None / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Max Volume (`passenger-009ae3a402da`), Dot Matrix (`passenger-3c18da10302b`), Bill Rush (`passenger-488bd780fe51`), Al Lee (`passenger-d9f5d0d1a6a6`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.RamTraffic
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/RamTraffic.asset`

Authored description: Ram one traffic car. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: Traffic; requires/forbids: Impact / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Ray Rage (`passenger-419497acb1af`), Joy Ride (`passenger-a492d1d13dab`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.HitPedestrian
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/HitPedestrian.asset`

Authored description: Hit a pedestrian. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: Pedestrians, PedestrianHitDetection; requires/forbids: Impact / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Robin Roundabout (`passenger-6571dd82a0e1`), Joy Ride (`passenger-a492d1d13dab`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.PropertyDamage
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/PropertyDamage.asset`

Authored description: Knock over roadside clutter. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: DestructibleProps; requires/forbids: Impact / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Ray Rage (`passenger-419497acb1af`), Joy Ride (`passenger-a492d1d13dab`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.Offroad
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/Offroad.asset`

Authored description: Spend 5 seconds offroad. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 5; requirements: Offroad; requires/forbids: Offroad / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Dot Matrix (`passenger-3c18da10302b`), Robin Roundabout (`passenger-6571dd82a0e1`), Al Lee (`passenger-d9f5d0d1a6a6`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.SmoothRide
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/SmoothRide.asset`

Authored description: Keep the ride smooth. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: Timer; requires/forbids: None / Impact.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Nora Brake (`passenger-0dc6acff58d9`), Pat Pending (`passenger-ca28f1b00a01`), Cam Era (`passenger-e85cc6083da9`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.NoCollisions
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/NoCollisions.asset`

Authored description: Arrive without a collision. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: Timer; requires/forbids: None / Impact.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Nora Brake (`passenger-0dc6acff58d9`), Pat Pending (`passenger-ca28f1b00a01`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.MaximumChaos
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/MaximumChaos.asset`

Authored description: Make 400 chaos points. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 400; requirements: Chaos; requires/forbids: Chaos / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Max Volume (`passenger-009ae3a402da`), Ray Rage (`passenger-419497acb1af`), Joy Ride (`passenger-a492d1d13dab`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.ScenicRoute
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/ScenicRoute.asset`

Authored description: Scenic stop. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: ScenicStops; requires/forbids: TimedStop / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Dot Matrix (`passenger-3c18da10302b`), Cam Era (`passenger-e85cc6083da9`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.NearMiss
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/NearMiss.asset`

Authored description: Thread a near miss. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: Traffic, NearMiss; requires/forbids: HighSpeed / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Cobalt Unit Seven (`analytical-robot`), Sterling Withers (`ancient-billionaire`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Unit Finch (`bird-loving-android`), Ari Wildcard (`card-game-hero`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Benny Wonder (`clueless-adult`), Sid Booth (`color-commentator`), Panel Pete (`comic-book-nerd`), Vic Mayhem (`crime-psychopath`), Sage Mossledger (`critical-old-mentor`), Syl Arrowlane (`fantasy-archer`), Borin Axleforge (`fantasy-dwarf`), Professor Oddroute (`fantasy-professor`), Donut Doug (`food-obsessed-dad`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Verdant Scowl (`grumpy-green-warrior`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Sam Checklist (`manual-comedy`), Champ Spotlight (`martial-arts-celebrity`), Kira Torque (`mechanic-inventor`), Baron Tollpaw (`merchant-bear`), Major Wingfield (`military-bird`), Tank Stripejaw (`muscle-animal`), The Wooden Adviser (`mystical-mask`), Rex Threewide (`nascar-superfan`), Doctor Quiver (`nervous-scientist`), Dirtstorm Dani (`offroad-lunatic`), The Velvet Attendant (`ominous-attendant`), Earl Switchback (`p001`), Casey Caution (`p002`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Miss Mabel Grace (`p006`), Martin Cadenza (`p007`), Gus Thunderseat (`p008`), Marvin No Tip Phelps (`p009`), Max Volume (`passenger-009ae3a402da`), Robin Roundabout (`passenger-6571dd82a0e1`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Fern Wayfinder (`sensible-faun`), Kip Laststand (`short-anxious-fighter`), Mim Plumb (`simulation-passenger`), Stu Drowsley (`sleepy-inventor`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Cora Circuit (`tech-hero`), Doctor Secondhand (`time-scientist`), Professor Loosebolt (`unstable-engineer`), Walt Eighteen (`veteran-trucker`), Rusty Encore (`washed-up-entertainer`)

## taxi.objective.IllicitStop
Asset: `Assets/LWS/TruckTaxi/ScriptableObjects/Requests/IllicitStop.asset`

Authored description: Make a sketchy pickup. Runtime text uses authoritative Target.

Enabled: True; deadline: 180s; base target: 1; requirements: IllicitStops; requires/forbids: TimedStop / None.

Authored eligible passengers: Rivet Starwrench (`alien-tinkerer`), Grint Cindersnout (`angry-fantasy-villain`), Prince Volt (`angry-power-warrior`), Bront Cheerstrike (`bald-chaos-warrior`), Bongo Cratefoot (`chaotic-marsupial`), Dash Stopwatch (`cheetah-challenger`), Vic Mayhem (`crime-psychopath`), Borin Axleforge (`fantasy-dwarf`), Grand Elder Grumble (`giant-alien-elder`), The Chicken (`giant-chicken`), Flicker (`hover-companion`), Grumble Gnash (`insult-sensitive-creature`), Pine Barrens Percy (`jersey-creature`), Kip Springstep (`kangaroo-jumper`), Munch Gravelgut (`large-roadside-creature`), Boulder Frostfoot (`large-yeti`), Roo Riot (`laughing-lunatic`), Officer Later (`lazy-cop`), Tank Stripejaw (`muscle-animal`), Rex Threewide (`nascar-superfan`), Dirtstorm Dani (`offroad-lunatic`), Earl Switchback (`p001`), Grant Deadline (`p003`), Bree Brightside (`p004`), Darius Fastlane (`p005`), Gus Thunderseat (`p008`), Max Volume (`passenger-009ae3a402da`), Dot Matrix (`passenger-3c18da10302b`), Ray Rage (`passenger-419497acb1af`), Bill Rush (`passenger-488bd780fe51`), Robin Roundabout (`passenger-6571dd82a0e1`), Joy Ride (`passenger-a492d1d13dab`), Al Lee (`passenger-d9f5d0d1a6a6`), Lord Courtesy (`polite-tyrant`), Chairman Whiskers (`property-cat`), Violet Emberwing (`purple-dragon`), Jett Sandrunner (`quiet-hero`), Rory Roar (`reptile-superfan`), Cassian Deckworth (`rich-card-rival`), Doctor Malfunction (`robot-villain`), Doctor Cranium (`scheming-scientist`), Perfect Specimen Nine (`smug-bio-villain`), Lane Sidestreet (`smuggler`), Captain Applause (`space-hero`), Agent Bananas (`space-monkey`), Coach Mack Playbook (`sports-commentator`), Buck Fender (`stock-car-veteran`), Jules Crossover (`street-baller`), Zip Chattertail (`talking-sidekick`), Professor Loosebolt (`unstable-engineer`), Rusty Encore (`washed-up-entertainer`)

